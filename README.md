# project-h2

## Commands

• Install dependencies (in `./`)
```
$ npm install
```

• Start server (in `./`)
```
$ node index.js
```

• Start & watch with nodemon (in `./`)
```
$ npm i -g nodemon
$ nodemon index.js
```

• Build and watch front JS (in `./public`)
```
$ webpack --watch
```
